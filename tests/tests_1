exit 42 world 
cat minishell.h | grep ");"$ 
grep hi "<infile" <         ./test_files/infile 
cat <"1""2""3""4""5" 
cat <missing
ls >./outfiles/outfile01 >./test_files/invalid_permission
echo hi | echo bye >./test_files/invalid_permission
ls >./test_files/invalid_permission >"./outfiles/outfile01" >./test_files/invalid_permission 
echo hi | >./outfiles/outfile01 echo bye >./test_files/invalid_permission 


echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
echo "Starting..." |
ls -l | grep "file" > output.txt |
echo "Total files found: $(wc -l < output.txt)" |
cat <<EOF > input.txt |
This is some text. 
It has multiple lines.
EOF 
cat < input.txt | sed 's/ /_/g' >> modified_input.txt ; |
export MY_VAR="Hello, World!"  |
echo "$MY_VAR"   |
unset MY_VAR 
cd /tmp && echo "Current directory: $(pwd)" |
echo "Done."
